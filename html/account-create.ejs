<% 
var alphabet = '0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-_'.split('')
  , length = 64
  , map = {}
  , seed = 0
  , i = 0
  , prev;

/**
 * Return a string representing the specified number.
 *
 * @param {Number} num The number to convert.
 * @returns {String} The string representation of the number.
 * @api public
 */
function encode(num) {
  var encoded = '';

  do {
    encoded = alphabet[num % length] + encoded;
    num = Math.floor(num / length);
  } while (num > 0);

  return encoded;
}

/**
 * Return the integer value specified by the given string.
 *
 * @param {String} str The string to convert.
 * @returns {Number} The integer value represented by the string.
 * @api public
 */
function decode(str) {
  var decoded = 0;

  for (i = 0; i < str.length; i++) {
    decoded = decoded * length + map[str.charAt(i)];
  }

  return decoded;
}

/**
 * Yeast: A tiny growing id generator.
 *
 * @returns {String} A unique id.
 * @api public
 */
function yeast() {
  var now = encode(+new Date());

  if (now !== prev) return seed = 0, prev = now;
  return now +'.'+ encode(seed++);
}                          %>
  
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Redirecting…</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="color-scheme" content="dark light" />
  <meta name="theme-color" content="#000000" />
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; connect-src 'self' https://poketube.fun; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; base-uri 'self'; form-action 'self'">
  <style>
    :root { --bg:#0b0b0b; --fg:#e8e8e8; --muted:#9a9a9a; --accent:#71c7ff; }
    html,body { margin:0; padding:0; height:100%; background:var(--bg); color:var(--fg); font:14px/1.4 system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Noto Sans", "Helvetica Neue", Arial, "Apple Color Emoji", "Noto Color Emoji", "Segoe UI Emoji"; }
    .wrap { min-height:100%; display:grid; place-items:center; padding:24px; }
    .card { width:100%; max-width:520px; border:1px solid #1c1c1c; border-radius:12px; background:#0f0f0f; box-shadow:0 0 0 1px #121212 inset, 0 8px 32px rgba(0,0,0,.5); }
    .inner { padding:20px 22px; }
    h1 { font-size:18px; margin:0 0 8px; }
    p { margin:8px 0 0; color:var(--muted); }
    .row { display:flex; align-items:center; gap:12px; margin-top:14px; }
    .spinner { width:16px; height:16px; border-radius:50%; border:2px solid var(--muted); border-top-color:var(--accent); animation:spin 0.9s linear infinite; }
    kbd { background:#171717; color:#dcdcdc; padding:2px 6px; border:1px solid #232323; border-radius:6px; font-size:12px; }
    @keyframes spin { to { transform:rotate(360deg); } }
    .small { font-size:12px; color:#7b7b7b; }
    .hidden { display:none !important; }
    a { color:var(--accent); text-decoration:none; }
    a:hover { text-decoration:underline; }
    
    /* New styles for forms */
    .form-group { margin-top: 16px; }
    .form-group label { display: block; font-size: 13px; margin-bottom: 6px; color: var(--fg); }
    input[type="text"] { width: 100%; padding: 10px; border: 1px solid #333; border-radius: 6px; background: #1a1a1a; color: var(--fg); font-family: inherit; box-sizing: border-box; }
    input[type="text"]:focus { outline: 2px solid var(--accent); border-color: transparent; }
    button.btn { width: 100%; padding: 12px; margin-top: 20px; border: none; border-radius: 6px; background: var(--accent); color: #000; font-weight: bold; cursor: pointer; font-family: inherit; font-size: 14px; transition: opacity 0.2s; }
    button.btn:hover { opacity: 0.9; }
    #captchaCanvas { width: 100%; height: 80px; background: #111; border: 1px solid #333; border-radius: 6px; margin-bottom: 8px; }
    .error-msg { color: #ff6b6b; font-size: 13px; margin-top: 10px; display: none; padding: 8px; background: rgba(255, 107, 107, 0.1); border-radius: 6px; border: 1px solid rgba(255, 107, 107, 0.2); }
  </style>
</head>
<body>
  <noscript>
    <div class="wrap">
      <div class="card">
        <div class="inner">
          <h1>JavaScript required</h1>
          <p>This page assigns you a user ID and redirects to your account.</p>
          <p class="small">Enable JavaScript and refresh, or go to <a href="/my-acc">/my-acc</a> and sign in manually.</p>
        </div>
      </div>
    </div>
  </noscript>

  <div class="wrap" id="app">
    <div class="card">
      
      <div class="inner" id="loading-state">
        <h1>Preparing your account…</h1>
        <div class="row"><div class="spinner" aria-hidden="true"></div><p>Checking authorization securely.</p></div>
      </div>

      <div class="inner hidden" id="setup-state">
        <h1>Human Verification Required</h1>
        <p>Before generating your user ID, please solve the captcha and agree to our <a href="https://poketube.fun/policies/" target="_blank">policies</a>.</p>
        
        <div class="form-group">
          <canvas id="captchaCanvas" width="300" height="80"></canvas>
          <label for="captchaInput">Enter the text shown above (case-sensitive):</label>
          <input type="text" id="captchaInput" autocomplete="off" placeholder="Type captcha here..." />
        </div>

        <div class="form-group">
          <label for="agreeInput">Type <strong>AGREE</strong> to accept all policies:</label>
          <input type="text" id="agreeInput" autocomplete="off" placeholder="AGREE" />
        </div>

        <div class="error-msg" id="error-msg">Incorrect captcha or agreement text. Please try again.</div>

        <button class="btn" onclick="verifyAndProceed()">Verify & Create Account</button>
      </div>

    </div>
  </div>

  <script>
    var apiurl = "https://poketube.fun/api";
    <% var userid = yeast() %>
    
    var currentUserId = localStorage.getItem("UserID");
    var currentCaptchaText = "";

     function generateCaptcha() {
      const canvas = document.getElementById('captchaCanvas');
      const ctx = canvas.getContext('2d');
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      // Background noise
      ctx.fillStyle = '#111';
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      // Random characters (removed visually ambiguous ones like I, l, 1, O, 0)
      const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789';
      currentCaptchaText = '';
      for (let i = 0; i < 6; i++) {
        currentCaptchaText += chars.charAt(Math.floor(Math.random() * chars.length));
      }

      // Draw intersecting noise lines
      for (let i = 0; i < 15; i++) {
        ctx.strokeStyle = `rgba(${Math.random()*255},${Math.random()*255},${Math.random()*255},0.4)`;
        ctx.lineWidth = Math.random() * 2;
        ctx.beginPath();
        ctx.moveTo(Math.random() * canvas.width, Math.random() * canvas.height);
        ctx.lineTo(Math.random() * canvas.width, Math.random() * canvas.height);
        ctx.stroke();
      }

      // Draw text with random size, position, rotation, and color
      for (let i = 0; i < currentCaptchaText.length; i++) {
        ctx.save();
        const fontSize = 30 + Math.random() * 15;
        ctx.font = `bold ${fontSize}px sans-serif`;
        // Position text evenly across width but random heights
        ctx.translate(20 + (i * 45), 45 + (Math.random() * 15 - 7)); 
        // Random rotation
        ctx.rotate((Math.random() - 0.5) * 0.8);
        ctx.fillStyle = `hsl(${Math.random() * 360}, 70%, 65%)`;
        ctx.fillText(currentCaptchaText[i], 0, 0);
        ctx.restore();
      }

      // Add foreground noise dots
      for (let i = 0; i < 80; i++) {
        ctx.fillStyle = `rgba(255,255,255,0.2)`;
        ctx.beginPath();
        ctx.arc(Math.random() * canvas.width, Math.random() * canvas.height, Math.random() * 2, 0, 2 * Math.PI);
        ctx.fill();
      }
    }

    // Main execution
    if (currentUserId) {
      // User is already logged in, redirect normally
      location.href = "/my-acc?ID=" + currentUserId + "&rparam=1";
    } else {
      // User is not logged in, show captcha form
      document.getElementById('loading-state').classList.add('hidden');
      document.getElementById('setup-state').classList.remove('hidden');
      generateCaptcha();
    }

    // Validate form and proceed with original logic
    function verifyAndProceed() {
      var captchaInput = document.getElementById('captchaInput').value;
      var agreeInput = document.getElementById('agreeInput').value;
      var errorMsg = document.getElementById('error-msg');

      if (captchaInput !== currentCaptchaText || agreeInput !== 'AGREE') {
        // Failed verification
        errorMsg.style.display = 'block';
        document.getElementById('captchaInput').value = '';
        generateCaptcha(); // Regenerate a new captcha to prevent brute-forcing
      } else {
        // Success verification
        errorMsg.style.display = 'none';
        
        // Execute the original assignment, DB set, and redirect
        localStorage.setItem('UserID', `<%- userid %>`);
        <% db.set(`user.${userid}`, userid) %>
        location.href = "/my-acc?ID=" + `<%- userid %>` + "&rparam=1";
      }
    }
  </script>
</body>
</html>